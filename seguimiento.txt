#generamos producto entity
generamos la tabla o clase, le damos un primary key autoincremental
generamos constructores, getters y setters
/
#generamos interface repository extendiendo JPARepository<tabla, tipo de id primary>
implementamos los metodos principales (find by name y exists?) 
https://www.arquitecturajava.com/que-es-un-java-optional/
/
#generamos service producto
inyectamos el repository producto
en vez de instanciar el constructor, con autowired, se encarga spring de crear una instancia del repositorio
implementamos los metodos CRUD
/
#generamos mensaje, para mostrar mensajes por pantalla en el cliente
/
#generamos producto dto, igual q producto pero sin el id
generamos los constructores y ponemos las restricciones, como @notblank para que no este vacio el nombre y @min para precio minimo
/
#generamos controlador
aca se agrega la url donde va a funcionar el front
inyectamos el servicio
generamos el restcontroller
